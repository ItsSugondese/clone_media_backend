services:
  myapp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: myapp_container
    image: clone-media:local-test-clone-media
    network_mode: host
    env_file:
      - .env
#    ports:
#      - "8080:8080"  # map your app's port to host
    restart: unless-stopped
